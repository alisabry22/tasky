import 'package:flutter/material.dart';

class OpenCalendar extends StatelessWidget {
  const OpenCalendar({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}